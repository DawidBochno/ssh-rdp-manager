__pycache__/
*.py[cod]
.venv/
venv/
env/
.idea/
.vscode/
# Zapisane połączenia (z hasłami w DPAPI) — nie trafiają do repo.
connections.json

# Wlasne skrypty uzytkownika — plik lokalny, nie repo.
scripts.json

# Notatki robocze dla Claude/deweloperów — nie dla uzytkownikow na GitHubie.
CLAUDE.md
TODO.md
